- name: python
  service: beltexam
  command: pip install -r requirements.txt
- service: awsdeployment
  command: aws ecs register-task-definition --cli-input-json file:///deploy/tasks/webtask.json
- service: awsdeployment
  command: aws ecs update-service --service ninjs-gold-test --task-definition webtask
